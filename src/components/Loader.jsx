import { Html, useProgress } from "@react-three/drei";

const Loader = () => {
  return (
    <Html>
      <span className="canvas-load"></span>
      <p style={{}}></p>
    </Html>
  );
};

export default Loader;
